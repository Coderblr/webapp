<div>
    <span style={styles.resultLabel}>
        Date Of Birth:
    </span>

    <span style={styles.resultValue}>
        {response.parsed.DATE_OF_BIRTH
            ? `${response.parsed.DATE_OF_BIRTH.slice(0, 2)}-${response.parsed.DATE_OF_BIRTH.slice(2, 4)}-${response.parsed.DATE_OF_BIRTH.slice(4, 8)}`
            : "-"
        }
    </span>
</div>