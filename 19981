# added to imports
import os

# added right after the imports (top-level)
for _stream in (sys.stdout, sys.stderr):
    try:
        _stream.reconfigure(encoding="utf-8", errors="replace")
    except (AttributeError, ValueError):
        pass

# added just before the behave Popen in main()
behave_env = {
    **os.environ,
    "PYTHONIOENCODING": "utf-8:replace",
    "PYTHONUTF8": "1",
}

# the Popen call itself
 process = subprocess.Popen(
-    behave_cmd, cwd=str(Path(__file__).parent),
+    behave_cmd, cwd=str(Path(__file__).parent), env=behave_env,
     stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True,
+    encoding="utf-8", errors="replace",
     **popen_kwargs,
 )
