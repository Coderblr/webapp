# ── Enterprise AI Automation Engineering Platform ──────────────────────────
# Python 3.11+

# Orchestration layer
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0
websockets==14.1

# AI workflow engine
langgraph==0.2.60
langgraph-checkpoint-sqlite==2.0.1
langchain==0.3.13
langchain-core==0.3.28
langchain-openai==0.2.14
langchain-community==0.3.13

# Database + vector store
# SQLite is used via Python's stdlib `sqlite3` module — no driver needed.
# Vector search is done in-process (backend/core/vectorstore.py), no pgvector.

# Browser / recorder (CDP driven, Edge)
selenium==4.27.1
websocket-client==1.8.0

# Templating / codegen
Jinja2==3.1.5

# Java source parsing for the Framework Indexing Engine
javalang==0.13.0

# Secrets at rest
cryptography==44.0.0

# BRD parsing
python-docx==1.1.2
pypdf==5.1.0
openpyxl==3.1.5

# Utilities
httpx==0.28.1
python-dotenv==1.0.1
tenacity==9.0.0
structlog==24.4.0
