import Head from "next/head";
import Layout from "@/components/Layout";
import RequireAuth from "@/components/RequireAuth";
import { Tabs } from "@/components/Tabs";
import RecordTab from "@/components/RecordTab";
import RunSavedTab from "@/components/RunSavedTab";
import MyExecutionsTab from "@/components/MyExecutionsTab";

export default function ToolPage() {
  return (
    <RequireAuth>
      <Layout>
        <Head>
          <title>Record &amp; Run - AI Test Recorder</title>
        </Head>

        <div className="mb-6">
          <h1 className="text-2xl font-semibold tracking-tight text-ink">Record &amp; Run</h1>
          <p className="mt-1 text-sm text-muted">
            Record a session on any web app, generate a Behave + Playwright test, then run it.
          </p>
        </div>

        <Tabs
          tabs={[
            { label: "Record & Generate", content: <RecordTab /> },
            { label: "Run Saved Recording", content: <RunSavedTab /> },
            { label: "My Executions", content: <MyExecutionsTab /> },
          ]}
        />
      </Layout>
    </RequireAuth>
  );
}
