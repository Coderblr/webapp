/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./pages/**/*.js", "./components/**/*.js"],
  theme: {
    extend: {
      colors: {
        // SBI-inspired palette: cool blue-white base, State Bank's signature
        // royal blue as the primary brand/action color, with a warm gold
        // secondary accent (echoing the keyhole mark) reserved for
        // highlights - not overused, so the blue stays dominant. Status
        // colors (pass/fail/online) are kept as conventional green/red so
        // they're never confused with the brand blue used for actions/links.
        paper: "#F4F7FC",
        panel: "#FFFFFF",
        ink: "#0B1E3D",
        muted: "#5B6B85",
        line: "#D8E1EF",
        rail: "#EAF1FA",
        brand: {
          blue: "#163C8C",
          blueDark: "#0E2A66",
          blueLight: "#E7EFFC",
          gold: "#C68A1E",
        },
        signal: {
          teal: "#1E8A5F",     // success / passed / online - kept green, not brand blue, so status is never mistaken for an action
          tealDark: "#146342",
          amber: "#C68A1E",
          rust: "#B3271E",
          slate: "#3B4E70",
        },
      },
      fontFamily: {
        sans: [
          "Inter",
          "Segoe UI",
          "ui-sans-serif",
          "system-ui",
          "-apple-system",
          "Helvetica Neue",
          "Arial",
          "sans-serif",
        ],
        mono: [
          "IBM Plex Mono",
          "SFMono-Regular",
          "Menlo",
          "Consolas",
          "Liberation Mono",
          "ui-monospace",
          "monospace",
        ],
      },
      boxShadow: {
        crisp: "0 1px 0 0 rgba(11,30,61,0.06)",
      },
    },
  },
  plugins: [],
};
