    code_match = re.search(r"\b(\d{4})\s+00\s+([A-Z][A-Z\s]+)", response)
    if code_match:
        return {
            "status": "error",
            "error_code": code_match.group(1),
            "error_description": code_match.group(2).strip()
        }
